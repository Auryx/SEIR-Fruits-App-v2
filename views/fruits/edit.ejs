<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head.ejs')%>
<body>
    <%- include('../partials/header.ejs')%>
    <main>
        <div>
            <form action="/fruits/<%=fruit._id%>?_method=PUT" method="POST" >
                <legend> Edit a <%= fruit.name%></legend>
                <fieldset>
                    <label>Name: <input type="text" name="name" placeholder="enter fruit name" value="<%= fruit.name %>"/></label>
                    <label>Color: <input type="text" name="color" placeholder="enter fruit color" value="<%= fruit.color %>"/></label>
                    <label>Ready to eat: 


                        <input 
                            type="checkbox" 
                            name="readyToEat" 
                            <%= fruit.readyToEat ? "checked" : "" %>
                        />


                    </label>
                    <input type="submit" value="Save Edit">
                </fieldset>
            </form>
        </div>
    </main>
</body>
</html>